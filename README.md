# Northstar Landing Page

A lightweight, responsive landing page built with semantic HTML and plain CSS.

## Files

- `index.html` — page structure, hero, CTA, approach section, and footer.
- `styles.css` — responsive layout, typography, color palette, and decorative hero artwork.

## Run locally

Open `index.html` directly in a browser, or serve the directory with any static web server.
